# @package _global_

defaults:
  - _self_
  - data: binding_site # choose datamodule with `test_dataloader()` for evaluation
  - model: binding_site
  - logger: null
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
